<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>MusicApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>

  </head>

  <body background="https://effectiviology.com/wp-content/uploads/The-Benefits-of-Music-and-Background-Noise.jpg">
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
   

    <% if user_signed_in? %>
      <%= link_to current_user.username, edit_user_registration_path %>
      <%= link_to 'Log out', destroy_user_session_path, :method => :delete%>
    <% else %>
      <%= link_to 'Log in', new_user_session_path, {:style=>"color: yellow;background:black;font-size:22px;font-weight:700;"} %>
      <%= link_to 'Sign up', new_user_registration_path, {:style=>"color: yellow;background:black;font-size:22px;font-weight:700;"} %>
    <% end %>
    <%= yield %>
  </body>
</html>


